data1 = load("../Data/HTBData/HTB2-mCitrine_SCD_M2.mat");
data2 = load("../Data/HTBData/HTB2_SCD_parameters_full.mat");

finalData = struct;

a = size(data1.pulsedata.totvolG1_daughters);
b = size(data2.pulsedata.totvolG1_daughters);

dim = min(a(2), b(2));

finalData.pulsedata.totvolG1_daughters = cat(1, a(:, 1:dim) b(:, 1:dim)));


